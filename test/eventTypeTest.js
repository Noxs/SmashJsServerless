const chai = require('chai');
const assert = chai.assert;
const expect = chai.expect;
const should = chai.should();
const EventType = require('../lib/core/eventType.js');

describe('EventType', function () {
    it('Test event instance', function () {
        //TODO 
    });
});
